//s,size() used to get the size of string nd strlen(ch) to get the length of char string

#include<bits/stdc++.h>
using namespace std;

int main(){
    string s1;
    s1 = "TAHMID";
    cout << s1.size() << endl;

    char ch[20] = "cggggggggg";
    cout << strlen(ch) << endl;

    return 0;
}